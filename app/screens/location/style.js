
import {
    StyleSheet
} from 'react-native';

const style = StyleSheet.create({
    featuresItem: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#eeeff0',
        padding: 20
    },
}
)

export default style;